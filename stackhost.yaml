user:
  name: ishikki
  github: ishikki-akabane

language: "python"
python_version: "3.13"

commands:
  build:
    - "pip install -r requirements.txt"
  package:
    - "nano"
  start:
    - "python3 bot.py"

env:
  - "ENVIRONMENT=production"
